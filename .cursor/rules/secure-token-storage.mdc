---
description: 安全存储令牌（禁止写入可提交配置）
alwaysApply: true
---

# 安全存储令牌

- 绝对不要把令牌直接写入 `satis.json` 或任何会被提交到 Git 的配置文件中（包括但不限于：`*.json`、`.env`、`auth.json` 等）。
- 令牌应该配置在执行 Satis 构建命令的服务器环境变量里，或配置到 Composer 的全局认证文件中（不进入仓库）。
- 若需要在构建/CI 中使用令牌，只通过环境变量注入（例如在运行命令前由 CI 注入），不要把令牌写回工作区文件。

